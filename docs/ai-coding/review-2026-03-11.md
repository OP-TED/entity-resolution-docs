# AI Coding Setup — Quality Review

**Date:** 2026-03-11
**Reviewer:** Claude (Opus)
**Scope:** All 9 files in the AI-assisted coding setup

---

## File-by-File Analysis

### 1. CLAUDE.md (Master Entry Point)

**Purpose checklist:**

- [x] Concise enough for context loading (176 lines)
- [x] References all agents
- [x] References all skills
- [x] Memory conventions clear
- [x] GitNexus rules present and deduplicated
- [x] Project-wide behaviour rules
- [ ] Mentions relationship with global `~/.claude/CLAUDE.md`
- [ ] Branch model / PR base branch documented
- [ ] `.gitignore` considerations for memory files

**Issues found:**

| # | Severity | Issue | Fix |
|---|----------|-------|-----|
| C1 | Warning | `# currentDate` hardcoded at bottom — will go stale, looks like a system artifact | Remove it; Claude Code injects the date automatically |
| C2 | Warning | No mention that the global `~/.claude/CLAUDE.md` (Meaningfy coding practices) complements this file | Add a note referencing the global instructions |
| C3 | Minor | No mention of the `develop` branch as PR target | Add to project description |
| C4 | Minor | "Interaction" section is thin (2 bullets) | Acceptable — agent-specific prompts have more detail |

---

### 2. ai-coding-runbook.md (Developer Runbook)

**Purpose checklist:**

- [x] Primary Mermaid diagram present
- [x] All phases described with agent assignments
- [x] Memory conventions documented
- [x] Commit/PR conventions
- [x] Model selection guide
- [x] Do's and don'ts
- [x] Getting started checklist
- [ ] Developer's role at each phase explicitly stated
- [ ] How to create a new epic from scratch
- [ ] Mermaid diagram logically consistent with text

**Issues found:**

| # | Severity | Issue | Fix |
|---|----------|-------|-----|
| R1 | Critical | **Mermaid diagram logic error:** Clarity Gate (`G`) sends "Yes" to both `H` (BDD) and `J` (Task Breakdown) simultaneously, implying parallelism. But the text describes Phase 2 (BDD) before Phase 3 (Tasks). BDD features should feed into task implementation, not run in parallel with task breakdown. | Fix the arrow: `G --> H --> I`, then `I --> J` (sequential, BDD before tasks) |
| R2 | Warning | **Phase numbering inconsistency:** Diagram has "Phase 1, 1b, 2, 3, 4" but text has "Phase 1, 2, 3, 4". Clarity Gate is inside Phase 1 in the text but separate in diagram. | Rename diagram subgraphs to match text sections |
| R3 | Warning | **Developer role missing.** The runbook describes what agents do but not what the developer does at each phase. A runbook should say "YOU do X, then the agent does Y". | Add a "Developer Actions" column to each phase table |
| R4 | Warning | **Getting started checklist** jumps to "read the active EPIC.md" without explaining how to create one from a Confluence work shape. | Add step: "To start a new epic, provide a work shape to the `epic-planner`" |
| R5 | Minor | **Model selection** lists "Complex implementation → Sonnet" and "Simple implementation → Sonnet" as separate rows — redundant. | Merge into one row |
| R6 | Minor | **The spec flow formula** `(a) + (b) + (f') → ...` is hard to read in a polished runbook. | Replace with a readable sentence or small diagram |
| R7 | Minor | **No mention of git worktrees** for parallel work (was in brainstorming notes). | Add as an optional productivity tip |

---

### 3. ai-coding-setup-guide.md (Setup Guide)

**Purpose checklist:**

- [x] File structure overview with diagram
- [x] CLAUDE.md explanation
- [x] Agent configuration details
- [x] Skills configuration
- [x] Memory configuration (3 types distinguished)
- [x] MCP server configuration
- [x] Repo adaptation guide
- [x] Troubleshooting
- [ ] Tool restrictions in summary table match actual agent files
- [ ] `settings.local.json` explained
- [ ] Claude Code installation prerequisites

**Issues found:**

| # | Severity | Issue | Fix |
|---|----------|-------|-----|
| S1 | Critical | **Tool restriction table is inaccurate.** States epic-planner has "Read-only + AskUserQuestion" but actual file has `Read, Grep, Glob, Bash, AskUserQuestion`. States gherkin-writer has "Read + Write (test files only)" but actual file has `Read, Write, Glob, Grep, Bash` with no file restriction. | Update table to match actual agent frontmatter |
| S2 | Warning | **`settings.local.json` mentioned in file structure** but never explained. Developers won't know what to put there. | Add brief explanation or point to Claude Code docs |
| S3 | Warning | **Agent persistent memory vs. epic/task memory** distinction is confusing. Section 5.2 mentions `.claude/agent-memory/<name>/` (auto-created) alongside our custom `.claude/memory/epics/`. | Add a comparison table making the distinction sharper |
| S4 | Minor | **No mention of Claude Code installation** as a prerequisite. | Add a prerequisites section |

---

### 4. epic-planner.md (Agent)

**Purpose checklist:**

- [x] Clear delegation description
- [x] Model: Opus
- [x] Skills: stream-coding + clarity-gate
- [x] Memory: project
- [x] EPIC.md template structure defined
- [x] Clarity Gate integration
- [x] Boundaries (what it does NOT do)
- [x] Interaction style guidance
- [ ] Tool list appropriate for read-only intent
- [ ] Architecture docs location specified

**Issues found:**

| # | Severity | Issue | Fix |
|---|----------|-------|-----|
| E1 | Warning | **Bash tool included** but agent "does not write implementation code." Bash could be used to read docs or run checks, but it's a broad permission for a planning agent. | Remove Bash; Glob+Grep+Read are sufficient for reading. If the agent needs to run a specific check, add it back with justification. |
| E2 | Warning | **EPIC.md template uses markdown headings inside a markdown code block.** The nested `## Status`, `## Description` etc. could confuse parsers or the agent itself when the template is injected into the system prompt. | Indent the template or use a clearer delimiter |
| E3 | Minor | **No explicit instruction on where to find architecture docs.** Says "Architecture docs from the docs repository" — should reference `docs/modules/` or equivalent. | Add: "Architecture docs are typically under `docs/modules/ROOT/`" |
| E4 | Minor | **No instruction to read MEMORY.md** before starting work. | Add as step in "Read all available inputs" |

---

### 5. gherkin-writer.md (Agent)

**Purpose checklist:**

- [x] Clear delegation description
- [x] Model: Sonnet
- [x] Memory: project
- [x] Gherkin examples
- [x] Quality checks checklist
- [x] Boundaries
- [ ] Tool list justified
- [ ] Feature file naming convention
- [ ] Reads MEMORY.md for conventions

**Issues found:**

| # | Severity | Issue | Fix |
|---|----------|-------|-----|
| G1 | Warning | **Bash tool included with no clear reason.** This agent writes Gherkin features and test data — it doesn't need to execute shell commands. | Remove Bash from tools |
| G2 | Warning | **No instruction to read MEMORY.md** for project conventions before writing features. | Add as first step in Core Behaviour |
| G3 | Minor | **No feature file naming convention specified.** Should it be `<capability>.feature`? `<epic>_<capability>.feature`? | Add naming convention guidance |
| G4 | Minor | **No validation that Clarity Gate passed** before starting work. The description says "Use after... Clarity Gate passed" but the agent doesn't check. | Add: "Verify EPIC.md status is 'Ready' before proceeding" |

---

### 6. implementer.md (Agent)

**Purpose checklist:**

- [x] Clear delegation description
- [x] Model: Sonnet
- [x] Skills: stream-coding
- [x] Memory: project
- [x] Generate-verify-integrate loop
- [x] Architectural rules
- [x] Rule of Divergence
- [x] Task completion memory writing
- [x] Boundaries
- [ ] Reads existing code before implementing
- [ ] Test-first guidance
- [ ] Handles trivial vs. design-level failures differently

**Issues found:**

| # | Severity | Issue | Fix |
|---|----------|-------|-----|
| I1 | Warning | **"Fix the spec, not the code" is too rigid.** Sometimes the spec is correct but generated code has a trivial bug (typo, off-by-one). The agent should fix trivial code bugs directly and only escalate to spec revision for design-level failures. | Add nuance: "For trivial bugs (typos, off-by-one), fix the code. For design-level failures (wrong approach, missing requirements), fix the spec." |
| I2 | Warning | **No instruction to read existing code** in relevant layers before implementing. The agent should understand what's already there to avoid duplication or conflicts. | Add step 6 in "Before You Start": "Read existing code in the affected layers" |
| I3 | Minor | **No mention of test-first vs code-first order.** Given BDD is already written by gherkin-writer, the agent should write step definitions first, then production code (outside-in TDD). | Add guidance on implementation order |
| I4 | Minor | **No mention of `importlinter`** or architectural validation tooling that could be run after generating code. | Add: "If available, run architectural validation (e.g., `make check-architecture`)" |

---

### 7. code-reviewer.md (Agent)

**Purpose checklist:**

- [x] Clear delegation description with "use proactively"
- [x] Model: Opus
- [x] Tools: read-only + Bash for tests
- [x] Memory: project
- [x] Comprehensive review checklist (5 categories, 22 items)
- [x] Output format (Critical/Warnings/Suggestions)
- [x] Boundaries
- [ ] Explicit Write/Edit deny
- [ ] Checks acceptance criteria from EPIC task
- [ ] Checks for spec-code divergence

**Issues found:**

| # | Severity | Issue | Fix |
|---|----------|-------|-----|
| V1 | Warning | **No explicit `disallowedTools: Write, Edit`** in frontmatter. The `tools` allowlist omits them, which should work, but explicit deny is safer and clearer for documentation. | Add `disallowedTools: Write, Edit, NotebookEdit` |
| V2 | Warning | **Spec Conformance section doesn't mention acceptance criteria.** Should specifically check each task's acceptance criteria from the EPIC.md task breakdown. | Add: "Acceptance criteria from the EPIC task breakdown are all met" |
| V3 | Minor | **No guidance on review scope.** "Run git diff" doesn't specify whether to review the current task's changes or the entire branch. | Add: "Review changes for the current task (use `git diff` for unstaged, `git diff --staged` for staged)" |
| V4 | Minor | **No divergence check.** Should verify that code matches the spec and flag any undocumented deviations as potential divergence. | Already partially covered in "Spec Conformance" — strengthen the language |

---

### 8. documenter.md (Agent)

**Purpose checklist:**

- [x] Clear delegation description
- [x] Model: Haiku
- [x] Skills: clarity-gate
- [x] Memory: project
- [x] Lightweight clarity checklist
- [x] Writing style guidance
- [x] Boundaries
- [ ] Reads MEMORY.md for context
- [ ] Docstring format specified
- [ ] Escalation path for complex tasks

**Issues found:**

| # | Severity | Issue | Fix |
|---|----------|-------|-----|
| D1 | Warning | **No instruction to read MEMORY.md first.** Should load project context before writing. | Add as first step |
| D2 | Minor | **No docstring format specified.** Python docstrings can be Google style, NumPy style, or reST. Should specify project convention. | Add: "Use Google-style docstrings unless the project uses a different convention" |
| D3 | Minor | **Haiku may be too weak for complex explanations.** No escalation path mentioned. | Add: "For complex code explanations requiring deep analysis, suggest the developer use a stronger model" |
| D4 | Minor | **Antora guidance is vague** ("follow Antora conventions"). | Add reference to `docs/antora.yml` for component structure |

---

### 9. MEMORY.md (Auto-Memory)

**Purpose checklist:**

- [x] Concise (28 lines, well under 200)
- [x] Placeholder sections for growth
- [x] Initial facts recorded
- [x] Branch model documented

**Issues found:**

No issues. Properly initialised.

---

## Summary of Issues by Severity

| Severity | Count | Files affected |
|----------|-------|----------------|
| Critical | 2 | Runbook (diagram logic), Setup Guide (tool table) |
| Warning | 13 | All files except MEMORY.md |
| Minor | 12 | Spread across all files |

## Cross-File Consistency Issues

| # | Issue | Files |
|---|-------|-------|
| X1 | Phase numbering differs between runbook diagram and text | Runbook |
| X2 | Tool restrictions in setup guide table don't match actual agent files | Setup Guide vs. all agents |
| X3 | "Read MEMORY.md first" instruction missing from 3 agents | gherkin-writer, epic-planner, documenter |
| X4 | Bash tool included in agents that don't need it | epic-planner, gherkin-writer |

---

## Improvements Applied

All issues of severity Critical and Warning were fixed. Minor issues were fixed
where the change was straightforward.

### Fixes by file

| File | Issues fixed | Changes |
|------|-------------|---------|
| **CLAUDE.md** | C1, C2, C3 | Removed stale `currentDate`; added global CLAUDE.md reference; added `develop` branch info |
| **ai-coding-runbook.md** | R1, R2, R3, R4, R5, R6, R7 | Fixed Mermaid diagram (sequential flow, no dual arrows); unified phase numbering; added developer-vs-agent table; added "start new epic" step; merged redundant model rows; replaced formula with prose; added worktrees tip |
| **ai-coding-setup-guide.md** | S1, S2, S3, S4 | Fixed tool restriction table to match actual agent files; added prerequisites section; added `settings.local.json` explanation; added comparison table for agent-memory vs epic/task memory |
| **epic-planner.md** | E1, E3, E4 | Removed Bash from tools; added MEMORY.md as first input; added architecture docs path |
| **gherkin-writer.md** | G1, G2, G3, G4 | Removed Bash from tools; added MEMORY.md reading; added feature file naming convention; added EPIC status check |
| **implementer.md** | I1, I2, I3, I4 | Added nuance to fix-spec-not-code (trivial vs design-level); added "read existing code" step; added test-first guidance; added importlinter mention |
| **code-reviewer.md** | V1, V2, V3, V4 | Added explicit `disallowedTools: Write, Edit, NotebookEdit`; added acceptance criteria check; clarified review scope (staged vs unstaged); strengthened divergence check |
| **documenter.md** | D1, D2, D3, D4 | Added MEMORY.md reading; specified Google-style docstrings; added escalation note for complex explanations; added Antora file references |
| **MEMORY.md** | — | No changes needed |
